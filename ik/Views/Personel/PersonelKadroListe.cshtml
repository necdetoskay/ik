@model IEnumerable<ik.Models.DataClasslari.PersonelKadroListeVM>

@{
    ViewBag.Title = "PersonelKadroListe";
}
<ul class="breadcrumb">
    <li class="breadcrumb-item">@Html.ActionLink("Personel Liste","TumPersonel")</li>
</ul>

<h2>Taşeron Personel Listesi</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdSoyad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Firma)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdSoyad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Firma)
        </td>
        <td>
           
        </td>
    </tr>
}

</table>
